<Window x:Class="RightMenuCheck.App.VersionStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RightMenuCheck 版本确认"
        Icon="Assets/RightMenuCheck.ico"
        Width="500"
        Height="290"
        MinWidth="500"
        MinHeight="290"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen">
  <Grid Margin="30,25">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel Orientation="Horizontal">
      <Image Width="40"
             Height="40"
             Source="/RightMenuCheck.App;component/Assets/RightMenuCheck.png" />
      <TextBlock Margin="13,0,0,0"
                 VerticalAlignment="Center"
                 FontSize="19"
                 FontWeight="SemiBold"
                 Text="暂时无法确认版本状态" />
    </StackPanel>
    <TextBlock Grid.Row="1"
               Margin="0,22,0,0"
               FontSize="14"
               Text="需要完成版本确认后继续使用。请检查网络连接，然后重试。"
               TextWrapping="Wrap" />
    <TextBlock x:Name="DetailText"
               Grid.Row="2"
               Margin="0,15,0,0"
               Foreground="{StaticResource TextSecondaryBrush}"
               TextWrapping="Wrap" />
    <Button Grid.Row="3"
            Width="112"
            Height="38"
            HorizontalAlignment="Right"
            Style="{StaticResource PrimaryToolbarButtonStyle}"
            Click="Retry_Click"
            Content="重试" />
  </Grid>
</Window>
