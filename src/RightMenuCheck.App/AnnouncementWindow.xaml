<Window x:Class="RightMenuCheck.App.AnnouncementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="RightMenuCheck 通知"
        Icon="Assets/RightMenuCheck.ico"
        Width="560"
        Height="390"
        MinWidth="520"
        MinHeight="340"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="28,24">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <TextBlock x:Name="KindText"
               Foreground="{StaticResource AccentBrush}"
               FontWeight="SemiBold" />
    <TextBlock x:Name="TitleText"
               Grid.Row="1"
               Margin="0,9,0,0"
               FontSize="20"
               FontWeight="SemiBold"
               TextWrapping="Wrap" />
    <ScrollViewer Grid.Row="2"
                  Margin="0,20,0,18"
                  VerticalScrollBarVisibility="Auto">
      <TextBlock x:Name="BodyText"
                 FontSize="14"
                 LineHeight="23"
                 TextWrapping="Wrap" />
    </ScrollViewer>
    <Button Grid.Row="3"
            Width="112"
            Height="38"
            HorizontalAlignment="Right"
            Style="{StaticResource PrimaryToolbarButtonStyle}"
            Click="Acknowledge_Click"
            Content="我知道了" />
  </Grid>
</Window>
